import { ReactElement } from "react";

import classes from "./styles.module.scss";

export default function Footer(): ReactElement {
  return <footer className={classes.footer}>Page footer</footer>;
}
